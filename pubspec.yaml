name: my_ganjoor_app
description: A Persian poetry reader
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  html: ^0.15.4

flutter:
  uses-material-design: true
  assets:
    - assets/backgrounds/